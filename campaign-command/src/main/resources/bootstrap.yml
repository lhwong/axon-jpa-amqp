server:
  port: 9002
spring:
  application:
    name: campaigncommand
  cloud:
    config:
      uri: http://localhost:8888
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30